apiVersion: v1
kind: Service
metadata:
  name: api-fm
  namespace: file-manager
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 9000
      nodePort: 30080
  selector:
    app: api-fm
